## 思路

从**右上角** `(0, n-1)` 出发（等价地，从左下角出发也可以）：

- 若 `matrix[i][j] == target`：找到了，返回 `true`；
- 若 `matrix[i][j] < target`：当前元素是第 `i` 行里最大的，这一行左边都不可能再有 `target`，于是整行排除，`i++`（往下走）；
- 若 `matrix[i][j] > target`：当前元素是第 `j` 列里最小的，这一列下边都不可能再有 `target`，于是整列排除，`j--`（往左走）。

为什么对：右上角这个位置很特殊——它同时是「所在行的最大值」和「所在列的最小值」，所以一次比较就能整行或整列地排除。每走一步至少排除一行或一列，最多走 `m + n` 步就会走出边界。这就是文档里 Java 解法的「切割数组」思路。

> 起点不能选左上角：左上角同时是最小值，无论 `>` 还是 `<` 都会错误地排除掉整行或整列。

## 复杂度

- 时间：O(m + n)，行指针只向下走 m 次、列指针只向左走 n 次
- 空间：O(1) 额外空间（存矩阵本身 O(mn)）

## 关键点

- 起点只能是右上角或左下角，这两个位置才同时具备「本行最大」「本列最小」（或反过来）的单调性
- 循环条件写 `i < m && j >= 0`，指针越界即说明不存在
- 输出是小写的 `true` / `false`：用 `boolalpha`，或者手写分支
- 元素可达 `±10^9`，用 `long long` 存，比较时别混入 `int` 造成窄化
- 元素可能重复，但本题的走法不依赖去重，遇到相等直接返回即可

## C++ 实现

见 [`solution.cpp`](./solution.cpp)：读入矩阵后从右上角一路「剥」到左下角。
